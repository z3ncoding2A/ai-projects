"""Execution layer (deterministic tools) for the sovereign voice assistant.

Run the daemon from the repo root:  python -m execution.daemon
Cross-module imports use the absolute form, e.g. `from execution.hypr import dispatch_exec`.
"""
