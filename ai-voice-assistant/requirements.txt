# Python dependencies for the sovereign voice assistant.
# REQUIRES Python 3.11 — openWakeWord/onnxruntime/ctranslate2/webrtcvad lack wheels for 3.13+.
# Build the venv with 3.11 explicitly:
#   uv venv --python python3.11 .venv && uv pip install --python .venv/bin/python -r requirements.txt
#   (or: python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt)
sounddevice>=0.4.6        # audio I/O (PortAudio)
numpy>=1.24
openwakeword>=0.6.0       # wake-word detection (ONNX)
onnxruntime>=1.17         # openWakeWord runtime (use onnxruntime-gpu for GPU wake inference)
faster-whisper>=1.0.0     # STT (CTranslate2). GPU needs CUDA 12 + cuDNN available
webrtcvad-wheels>=2.0.14  # voice activity detection (prebuilt wheels; imports as 'webrtcvad')
anthropic>=0.40.0         # cloud LLM (tool-calling + streaming)
python-dotenv>=1.0.0      # load ANTHROPIC_API_KEY from .env
PyQt6>=6.6                # overlay UI
# System packages (NOT pip): piper (TTS binary), alsa-utils (aplay), hyprland (hyprctl),
# and a Piper voice model. See directives/hyprland_control.md and directives/transcription.md.
